<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.electricityproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElectricityProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".person.dropshipment.DropShipmentActivity"></activity>
        <activity android:name=".shopp.orderdetails.OrderDetailsActivity" />
        <activity android:name=".shopp.userinfo.BindUserInfoActivity" />
        <activity android:name=".person.findforpay.FindForPayActivity" />
        <activity android:name=".details.DetailsActivity" />
        <activity
            android:name=".main.MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>